spring:
  jpa:
      hibernate:
        ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:h2:file:~/spring-boot-h2-db
    password:
    driver-class-name: org.h2.Driver
    username: sa
  port: 8080
